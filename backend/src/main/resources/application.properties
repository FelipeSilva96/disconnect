# ==============================================================
# CONFIGURA��ES DE CONEX�O COM O POSTGRESQL
# ==============================================================
spring.datasource.url=jdbc:postgresql://localhost:5432/disconnect_db

# O usu�rio padr�o do PostgreSQL (geralmente � postgres)
spring.datasource.username=postgres

spring.datasource.password=uma_senha_qualquer

# ==============================================================
# CONFIGURA��ES DO SPARK
# ==============================================================
app.port=8080
app.cors.allowed-origin=http://localhost:5173
app.auth.secret=disconnect-dev-secret-change-me

# ==============================================================
# CONFIGURA��ES DO FLYWAY
# ==============================================================

spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# ==============================================================
# CONFIGURA��ES DO HIBERNATE (JPA) // Se for permitido no futuro (ainda em aberto)
# ==============================================================

spring.jpa.hibernate.ddl-auto=validate

# Mostra no terminal os comandos SQL que o Java est� executando (�timo para estudos)
spring.jpa.show-sql=true

# ==============================================================
# CONFIGURACOES DA IA - ANTHROPIC / CLAUDE
# ==============================================================
# Configuração no ambiente:
# ANTHROPIC_API_KEY=sk-ant-...
anthropic.model=claude-haiku-4-5-20251001